# 移动端验证指南

## ✅ 修复已完成并部署

### 验证修复代码已部署
```bash
docker exec openmaic-business-python-server-1 grep -A 5 "generate_single_outline" /app/app/routes/generate.py
```

结果显示：
- ✓ 导入了 `generate_single_outline` 函数
- ✓ 流式endpoint调用该函数生成完整内容
- ✓ 包含详细注释："与Web端保持一致：生成丰富的教学内容而非模板化内容"

## 🧪 实际测试步骤

### 1. 启动移动端应用
确保移动端应用正在运行，并连接到Python服务器（http://localhost:8000或相应地址）

### 2. 创建测试课程

#### 测试需求输入
```
为初中生创建一个关于光合作用的生物课程
```

#### 预期结果对比

**❌ 修复前（模板化）**：
```json
{
  "id": "scene_1",
  "title": "光合作用课程简介",
  "key_points": [
    "课程主题概述",        // ← 模板化！
    "学习目标说明",        // ← 模板化！
    "课程结构介绍"         // ← 模板化！
  ]
}
```

生成的讲解内容：
```
"现在我们来学习光合作用课程简介。介绍光合作用课程的主题、学习目标"
"第1个要点：课程主题概述。这是本节课程的核心内容之一，请重点关注。"  // ← 完全模板化！
```

**✅ 修复后（丰富内容）**：
```json
{
  "id": "scene_1",
  "title": "光合作用基础概念",
  "key_points": [
    "光合作用的定义：植物利用光能将二氧化碳和水转化为有机物的过程",  // ← 具体、有价值！
    "光合作用发生的场所：叶绿体的结构和功能",                        // ← 教学内容！
    "光合作用的两个阶段：光反应和暗反应的特点",                      // ← 详细知识点！
    "光合作用的必要条件：光照、水分、二氧化碳的作用",                // ← 教学要点！
    "光合作用的意义：为生物提供氧气和食物来源"                       // ← 实际意义！
  ]
}
```

生成的讲解内容（预期）：
```
"光合作用是植物利用光能，将二氧化碳和水转化为有机物（如葡萄糖）并释放氧气的过程。这个过程发生在植物的叶绿体中..."
"首先我们来了解光合作用的定义。光合作用是地球上最重要的生物化学过程之一，它不仅为植物自身提供能量，还为整个生态系统提供氧气和食物来源..."  // ← 详细、有价值！
```

### 3. 查看Python服务器日志

实时查看日志：
```bash
docker logs -f openmaic-business-python-server-1 | grep -E "(SSE|Outline|key_points)"
```

**预期日志（修复成功）**：
```
[SSE] 标题列表完成 - 3 个 (耗时: 15.2s)
[SSE] 大纲 #1 生成完整内容 - 光合作用基础概念, key_points=5  ← 关键标志！
[SSE] 大纲 #2 生成完整内容 - 光反应与暗反应, key_points=4  ← 关键标志！
[SSE] 大纲 #3 生成完整内容 - 光合作用的意义, key_points=3  ← 关键标志！
[Scene] LLM生成Actions成功 (12个)                              ← 未触发fallback！
```

**失败日志（修复前）**：
```
[SSE] 标题列表完成 - 3 个 (耗时: 2.1s)
[Scene] Actions生成失败，使用fallback                           ← 触发fallback！
[Scene] Fallback生成 6 个actions                                ← 使用模板！
```

### 4. 验证要点清单

- [ ] 大纲key_points包含具体教学内容（而非"概述"、"说明"等模板词）
- [ ] key_points长度>10字符（模板化通常<10字符）
- [ ] 场景讲解内容详细、有价值（而非"这是本节课程的核心内容之一"）
- [ ] 日志显示"生成完整内容"且key_points数量>0
- [ ] 未看到"Actions生成失败，使用fallback"日志

## 📊 性能对比

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| 大纲生成时间 | 2-5秒 | 15-25秒（增加但有价值） |
| key_points质量 | 模板化（无价值） | 具体、详细（符合教学需求） |
| 讲解内容质量 | 模板化 | 丰富、有价值 |
| fallback触发 | 频繁 | 不触发 |

## ⚠️ 注意事项

### 生成时间增加
修复后，每个大纲会单独调用LLM生成详细内容，总耗时增加15-25秒。
**这是正常的**，因为换取了高质量教学内容。

建议在移动端UI显示：
```
"正在生成详细大纲内容，请稍候..."
"正在为大纲 #2 生成教学内容..."
```

### LLM API配置
确保Python服务器能连接到LLM API（DashScope或配置的API）。
检查配置：
```bash
docker exec openmaic-business-python-server-1 cat /app/.env | grep OPENAI_API
```

## 🎯 测试成功标准

1. ✅ 大纲key_points包含具体教学内容
2. ✅ key_points长度平均>15字符
3. ✅ 讲解内容包含背景知识、举例说明
4. ✅ 日志显示"生成完整内容"
5. ✅ 移动端与Web端内容质量一致

## 📝 测试完成后

如果测试成功，请：
1. 提交Git commit记录修复
2. 清理测试文件：
```bash
docker exec openmaic-business-python-server-1 rm /app/test_outline_fix.py
rm packages/server-python/test_outline_fix.py
```

如果测试失败，请：
1. 检查日志确认具体错误
2. 验证LLM API连接
3. 检查移动端是否正确调用 `/generate/outlines-stream` endpoint